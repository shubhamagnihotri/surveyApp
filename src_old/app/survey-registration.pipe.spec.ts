import { SurveyRegistrationPipe } from './survey-registration.pipe';

describe('SurveyRegistrationPipe', () => {
  it('create an instance', () => {
    const pipe = new SurveyRegistrationPipe();
    expect(pipe).toBeTruthy();
  });
});
